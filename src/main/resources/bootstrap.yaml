spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: false
      paths: /mnt/secrets/fact
      aliases:
        fact-api-POSTGRES-HOST: POSTGRES_HOST
        fact-api-POSTGRES-PASS: POSTGRES_PASSWORD
        fact-api-POSTGRES-DATABASE: POSTGRES_DATABASE
        fact-api-POSTGRES-USER: POSTGRES_USER
